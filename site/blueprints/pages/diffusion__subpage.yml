blueprint: page

title: Les diffusions d'événements

icon: ticket

options:
  delete:
    admin: true
    reader: false
  preview: "https://grutli.ch/{{ page.parent.slug }}/{{ page.slug }}"

columns:
  - width: 1/1
    sections:
      listed:
        label: Pages
        type: pages
        layout: cards
        image:
          cover: true
        template:
          - diffusion
